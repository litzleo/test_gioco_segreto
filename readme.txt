tu sei il rettangolo rosso
se cadi di lato cadi all'infinito
se riesci a morire (prova a scoprire come) c'è solo un fastidioso alert, era solo per testare la morte. L'aspect ratio è super a caso, ma non è un problema cambiarlo.

Comandi:
non ho ancora il menù del rebinding, per ora usa wasd o le freccette (con l'alternativa di poter saltare con barra spaziatrice), però il codice è già ben impostato per poter fare i rebind

ultima cosa a destra non c'è niente, la piattaforma continua ma non ci sono ostacoli

a già, ovvietà ma ti basta aprire index.html con qualsiasi browser